- defaults:
    link:
      create: true
      relink: true

- clean: ["~"]

- link:
    ~/.bash_profile: config/bash_profile
    ~/.bashrc: config/bashrc
    ~/.editorconfig: config/editorconfig
    ~/.gitconfig: config/gitconfig
    ~/.gitignore: config/gitignore
    ~/.local/bin/diff-so-fancy: bin/diff-so-fancy
    ~/.local/share/fonts/DejaVuSansMono: fonts/DejaVuSansMono
    ~/.oh-my-zsh: oh-my-zsh
    ~/.oh-my-zsh/custom/themes/spaceship.zsh-theme: spaceship-prompt/spaceship.zsh-theme
    ~/.shell: shell
    ~/.shell_env: config/shell_env
    ~/.xinitrc: config/xinitrc
    ~/.zshrc: config/zshrc

- shell:
    - [git submodule update --init --recursive, Installing submodules]
